# Optional local development integrations only.
# Copy to .env locally and never commit real secrets.

VLLM_BASE_URL=http://127.0.0.1:8000
COMFYUI_BASE_URL=http://127.0.0.1:8188

# Keep empty unless an explicitly approved integration requires it.
EXTERNAL_API_KEY=
